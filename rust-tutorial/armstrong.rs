fn main() {
    let mut v = vec![0, 0];
    v.push(1);
    v.push(2);
    for x in 0..v.len(){
        println!("{}", v[x]);
    }
}         

println!("length is {}", vec1.len());
for x in 0..vec1.len() {
    println!("{} {}", x, vec1[x]);
}
}